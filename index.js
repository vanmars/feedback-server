const express = require('express');
const app = express();

// Create route handler
app.get('/', (req, res) => {
  res.send({hi: 'there'});
});


// Create route handler
app.get('/', (req, res) => {
  res.send({hi: 'there'});
});